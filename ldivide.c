#include <stdio.h>
void main()
{
    int n;
    printf("enter the number: ");
    scanf("%d", &n);
    if (n % 7 == 0)
        
    printf("the number is divisible");
    else
    {

        printf("the number is not divisible");
    }
}