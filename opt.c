// wap to find product of two given number without using operator.

#include <stdio.h>
void main()
{
    int i, product = 1, a,b;
    printf("enter a number: ");
    scanf("%d%d", &a, &b);
    for (i = 1; i <= 5; i++)
        ;
    {
        product = product * a;
    }
    printf("the multiple of the number is %d", product);
}